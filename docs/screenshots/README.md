# Screenshots

Put PNG screenshots here so they show up in the main README:

- `dashboard.png` — Dashboard tab (status, live node pings, traffic)
- `routing.png` — Routing tab (mode, community lists, per-client)
- `diagnostics.png` — Diagnostics tab (service state, VPN-exit test, logs)

Open **LuCI → Services → VPN Pool** and capture each tab.
